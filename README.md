# Backend

This folder contains the backend copy of the ticket-booking project.

Structure mirrors the original project under `src`.

To initialize and push (example):

```powershell
cd src\backend
git remote add origin <git-remote-url>
git push -u origin main
```
